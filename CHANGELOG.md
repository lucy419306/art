# 更新日志

## 2026-09-12（P7 留白）

- P7 检测句与选项卡之间，由 1 秒硬切黑屏改为四拍留白＋轻度信号不良：压黑 0.3s → 干扰 1.2s（柔边横带 + 复用 grain + 一次微弱亮度起伏 + `glitch.mp3` 轻声占位）→ 纯净留白 0.4s → 回场 0.4s。残句槽默认空，不自编字。
- 留白段内无语音；「白键删除记忆，黄键保留记忆。请选择。」仍在回场后、与选项卡同现。选择 / 超时 / P8 判定未改。
- 新建 `游戏脚本v1.7.md`（仅记 P7 留白）；`游戏脚本v1.6.md` 未动。

## v1.6.0 — 2026-09-12

汇总 v1.2–v1.5 残缺旧稿与此后已落地实现，整合成完整现行《游戏脚本v1.6.md》。旧稿保留作谱系，演出以 v1.6 为准。

脚本对齐的现行口径（旧稿未写全或写错）：

- **键位**：白／黄／红空心键帽；P0 三键均可开场（红＝空格）；按页只显示可用键。
- **文案**：用户 **6431**；P2「你的人生已无需由你决定。」；P6「婚姻匹配度」。
- **P8**：等宽「正在评估自主决策能力」＋极慢光点，停留 5 秒。
- **P3**：累计行后两句字幕下移。
- **P9C／C**：白＝A2，黄＝证书；白线随交接台词展开；按任意键或 15 秒接受；朱红落印；「未来结果：无法预测」加大加横线。
- **P9R**：报告页后抉择页 15 秒红条；无「请保持原位」。
- **P11**：同骨架暖琥珀，2.4 秒入场与 `switch` 占位。
- **复位**：结局后 30 秒回 P0；P9C 无应答仍 90 秒进 A2。

## 2026-09-12（续）

- 结局演出结束后自动复位由 90 秒改为 30 秒（`resetTimeout`）。P9C 无应答仍为 90 秒进 A2，未改。
- 结局 C 证书：角标、发丝双边框、书写扫描线、印章双环与签收光线重做，贴合评估室科幻语系；正文与流程未改。
- 结局 C 证书再修：白线在「最终确认完成 / 自主决策权交接」台词期间展开为证书；等待接收显示「按任意键接受证书」，15 秒无操作自动接受；「自主决策权 / 已交接」改为朱红落印并加重动效；「未来结果：无法预测」加大并加左右横线。剧本正文未改。
- 全流程 UI 批次 1 大部分回退。仅保留 P8：等宽「正在评估」＋光点极慢脉动，停留改为 5 秒。P3 两句字幕下移，避免贴住「累计替代决策」栏。P0 红键（空格）可开场。
- 文案：用户编号改为 `#A7K-8426`（欢迎语与证书申请人/接收方）；P2 第二卡去掉句首「现在，」；P6 两卡「匹配度」改为「婚姻匹配度」。未改脚本版本。
- 用户编号再改为 `用户6431`（欢迎语与证书）。
- P10B 三条契约改为完整句（屏幕与语音一致）：选错的人／陷入贫困／深夜反复出现痛苦记忆。`游戏脚本v1.6.md` 已同步。

## v1.5.0 — 2026-09-12

相对 v1.4 实现与脚本的已落地改动（剧情、分支、数值未改写，除已批准的 P9C 路由勘误）。

- **键位语言**：全片统一白 / 黄 / 红；科幻空心键帽（无箭头、无填充）＋右侧「白键 / 黄键 / 红键」标识。P0 底部三键提示（红键开场无效）；P5–P7、P9C 为白＋黄；P9R 仅红；P10B 三键；犹豫浮层仅白＋黄；其余页不显示键位。
- **P9C**：大框改为大档键组件（白＝放弃，黄＝恢复）。路由勘误：白键 → A2（「恢复程序已终止。」）；黄键 → 结局 C（证书交接，无需再次确认）。
- **P9R 抉择页**：中央「若想强制拥有决策权 / 可按下红键」；进度条居中加粗，上方「请做出抉择」，15 秒排空 → A1；按红键 → P10B。去掉抉择页「申请已驳回」与右下角小字。
- **P11（A1/A2/A3 共同部分）**：去掉阳光窗景换皮；保留同一套科幻版式，加暖琥珀氛围层、暖边排期卡片逐条点亮、2.4 秒交叉入场与收尾收束。电流切换音挂点 `assets/sfx/switch.mp3`（缺文件则静默占位）。
- **开发者模式**（`npm run dev`）：拍跳转、静音、×1/×2/×4 加速、暂停；`npm start` 不带入。
- **工程**：Windows 源码启动与 `.gitignore` 排除构建产物；发布约定见 `RELEASING.md`。当时脚本正文见 `游戏脚本v1.5.md`（已由 v1.6 取代）。

## v1.4.0 — 2026-09-12

- 证书交接仪式（结局 C）、颜色键位初版、契约 / 犹豫 / 三入口结局 A。
- 静默回退与计时测试；台词与素材槽位清单。

## v0.1.0 — 2026-09-12

- 首个内测构建（macOS Apple Silicon）
- 基础版全流程可玩（含各结局与复位）
